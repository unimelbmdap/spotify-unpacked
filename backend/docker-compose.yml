services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: mediaflux-donation-backend:dev
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_URL: "sqlite+aiosqlite:////app/data/donations.db"
