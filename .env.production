# Production build (vite build). Same-origin API: the SPA calls /api and /admin
# on the host that serves it (Caddy reverse-proxies them to the backend).
# Empty value = relative URLs. Not loaded by dev (`vite dev`) or tests.
VITE_API_BASE_URL=
